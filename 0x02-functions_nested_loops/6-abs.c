#include "main.h"

/**
 * _abs - Computes the absolute value of intergers
 * @n: int to be examined
 *
 *Return: Absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
