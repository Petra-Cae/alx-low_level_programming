#include "main.h"
/**
 * add - Addition of two ints
 * @num: Number to be added
 * @nm: Number to be added
 *
 * Return: num plus nm
 */
int add(int num, int nm)
{
	int sum;

	sum = num + nm;
	return (sum);
}
