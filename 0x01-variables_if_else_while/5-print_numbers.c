#include <stdio.h>

/**
 * main - Print single digit numbers of base ten
 *
 * Return: Always 0
 */
int main(void)
{	
	t = 0
	for (t = 0; t < 10; t++)
		printf("%d", t);
	printf("\n");
	return (0);
}
